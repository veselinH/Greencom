spring:
  application:
    name: loyalty-service
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: 'false'
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:loyalty_test;DB_CLOSE_DELAY=-1
    username: sa
    password: ''
loyalty:
  scheduling:
    bonus-cron: "-"
    cache-evict-delay-ms: 3600000
